# Database Configuration
DB_USER=postgres
DB_HOST=localhost
DB_NAME=mysop
DB_PASSWORD=postgres
DB_PORT=5432

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3030

# JWT Configuration (if you implement authentication later)
# JWT_SECRET=your_jwt_secret
# JWT_EXPIRES_IN=24h 